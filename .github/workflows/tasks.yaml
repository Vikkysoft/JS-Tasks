name: GitHub Actions
on: [ push ]
jobs:
  Test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: 'npm start'
      - main: ' another command to run Javascript code'